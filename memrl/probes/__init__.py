"""Representation probes for the Memory × Exploration study.

decode_memory.py — measures how linearly-decodable the task-relevant memory is
from a frozen recurrent state, across training and across arms (none / e3b /
pbim / penalty). The central measurement behind the paper's thesis that an
exploration bonus *trains the memory*.
"""
